{
  "name": "odine-e2e-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.51.1"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
